#  Simple Weather App (Python)

A simple Python application that uses the **OpenWeather API** to show the current weather and temperature for any city.

## Features
- Real-time weather data
- Temperature in Celsius
- Simple and beginner-friendly code
- Uses the `requests` library and OpenWeather API

##  Requirements
Install the requests library:

```bash
pip3 install requests
